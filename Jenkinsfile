pipeline {
    agent any
    stages {
        stage('Example') {
            steps {
                echo 'verificando conexao'
                echo "Running ${env.BUILD_ID} on ${env.JENKINS_URL}"
            }
        }
    }
}